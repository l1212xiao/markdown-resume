核心：
在项目根目录下：
yarn build
yarn start

可能需要：
yarn --version
ll -a
yarn build
npm install chalk
yarn upgrade caniuse-lite browserslist
yarn build
npm install -g vue-cli
yarn install
yarn start

启动项目地址：
localhost:3000